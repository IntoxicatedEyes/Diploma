 '''
#Из задания 1 для моей дипломной работы может пригодиться функция нахождения нахождения среднего значения. (Среднее (mean) = сумма / количество)
sample = [4, 8, 15, 16, 23, 42]
def mean(values: list[float]) -> float:
    """Среднее арифметическое. Требует непустой список."""
    if len(values) == 0:
      print(('Введите данные в список'))
      my_input = float(input())
      values = [float(my_input)]
    elif len(values) >= 1:
      return sum(values) / len(values)
    # : проверьте пустой список и верните sum(values)/len(values)
    raise NotImplementedError("implement mean")

print("mean =", mean(sample))
'''
'''
#Из задания 1 для моей дипломной работы может пригодиться функция выведения отчета. 

def describe(values: list[float]) -> dict:
    """Короткое описание выборки (как мини-отчёт)."""
    #  верните dict c ключами n/min/max/mean/median/std (std только если len>=2)
    return {
       # 'n': len(values),
       # 'min': min(values),
       # 'max': max(values),
      #  'mean': mean(values),
      #  "median": median(values),
      #  "std": std_sample(values) if len(values) >= 2 else None,
    }
    raise NotImplementedError("implement describe")

print("describe(sample) =", describe(sample))
print("describe(sample_out) =", describe(sample_out))

'''
'''
Из задания 2:

Библиотека NumPy (Numerical Python) — это фундаментальная библиотека для научных вычислений в Python.
import numpy as np
# TODO: сгенерируйте 750 значений нормального распределения

#Она предоставляет мощный, многомерный массивный объект (ndarray), который позволяет эффективно работать с большими массивами данных.
# NumPy оптимизирован для выполнения численных операций, таких как сложение, умножение, преобразования и многие другие, намного быстрее, чем стандартные списки Python.
digits = np.random.normal(0, 1, 750)
#0 - среднее значение; 1 - стандартное отклонение; 750 - количество значений нормального распределения
print(len(digits))
'''

'''
# TODO: вычислите mean и std

mean = np.mean(digits) # Вычисляем среднее значение (арифметическое среднее) для нашего набора данных 'digits'.
std = np.std(digits)   # Вычисляем стандартное отклонение для нашего набора данных 'digits'. Стандартное отклонение показывает, насколько сильно данные отклоняются от среднего.
mean, std

#В данном случае:
#   - среднее (loc) = 0: Большинство чисел будут сгруппированы вокруг нуля.
#   - стандартное отклонение (scale) = 1: Определяет разброс чисел. Чем больше значение, тем шире разброс.
#   - количество_чисел (size) = 750: Сколько случайных чисел будет сгенерировано.
'''

'''
import matplotlib.pyplot as plt
# В данном контексте plt — это общепринятое сокращение (или алиас), используемое для импорта модуля pyplot из библиотеки matplotlib.
# TODO: постройте гистограмму data
# Matplotlib — это самая популярная библиотека для визуализации данных в Python. Она позволяет создавать графики, диаграммы и любые другие визуальные представления данных.
# hist() - для гистограммы;	pie() - круговые; bar(), barh() - столбцы, сравнение категорий
# bins = 50 - это количество столбцов
plt.hist(digits, bins = 50)
'''

'''
# TODO: добавьте выброс 10
digit_out = np.append(digits, 10)
# Выброс в статистике — это значение в наборе данных, которое резко отличается от остальных значений. Простыми словами, это то, что «выпадает» из общего ряда.
# Добавляем значение 10 в конец массива 'data', создавая новый массив 'data_out'. Значение 10 в данном контексте является выбросом, так как оно сильно отличается от остальных значений в нормальном распределении.
len(digit_out)
#В данном контексте 10 - это не число, а всего 1 элемент, поэтому, на выходе у нас не 760, а 751
'''